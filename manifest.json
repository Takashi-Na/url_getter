{
  "manifest_version": 2,
  "name": "Tab List Logger",
  "version": "1.0",
  "description": "Logs a list of open tabs to the console when a tab is loaded",
  "permissions": ["tabs"],
  "background": {
    "scripts": ["scripts/background.js"],
    "persistent": false
  }
}
